<div class="text-center">
    <h2>{{title}}</h2>
    <p>{{content}}</p>
    <p> Created by {{user.username}} on {{format_date createdAt}}</p>

    {{!-- put form in a partial --}}
    <form class="form new-comment-form">
        <input type="hidden" id="blogpost_id" name="blogpost_id" value="{{id}}" />
        <div class="form-group">
            <label for="add-comment">Comment</label>
            <input class="form-input" type="text" id="add-comment" name="add-comment"/>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary">Add Comment</button>
        </div>
    </form>
</div>

{{#if comments.length}}
<div class="col-md-6 comment-list">
    <h3>Comments</h3>

    {{#each comments as |comment|}}
    <div class="row mb-2">
        <div class="col-md-8">
        {{#if comment.createdAt}}
            <h4>{{comment.user.username}}</h4>
            <p>{{comment.comment}}</p>
            <p>On {{format_date comment.createdAt}}</p>
        {{else}}
            <p>No date available</p>
        {{/if}}
        </div>
    </div>
    {{/each}}
</div>
{{/if}}

<script src="/js/addComment.js"></script>
{{!-- Write the above script and see if comments should be moved to a partial --}}